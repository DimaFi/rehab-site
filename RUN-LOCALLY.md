# Как запустить сайт локально

## Быстрый запуск

### Вариант 1: Через Hugo напрямую

**Windows PowerShell:**
```powershell
cd D:\Code\rehab-site\rehab-site
.\hugo-bin\hugo.exe server
```

**Результат:**
- Сайт будет доступен по адресу: `http://localhost:1313`
- Автоматическая перезагрузка при изменениях файлов

---

### Вариант 2: С автоматическим открытием в браузере

```powershell
cd D:\Code\rehab-site\rehab-site
.\hugo-bin\hugo.exe server -D
```

Флаг `-D` включает черновики (draft) в отображение.

---

## Полезные параметры

### Запуск на другом порту
```powershell
.\hugo-bin\hugo.exe server --port 8080
```

### Запуск для сети (доступен с других устройств)
```powershell
.\hugo-bin\hugo.exe server --bind 0.0.0.0
```

### Запуск с отключенной перезагрузкой (быстрее)
```powershell
.\hugo-bin\hugo.exe server --disableFastRender
```

### Запуск с включением черновиков
```powershell
.\hugo-bin\hugo.exe server -D
```

---

## Проверка перед запуском

### 1. Проверка версии Hugo
```powershell
cd D:\Code\rehab-site\rehab-site
.\hugo-bin\hugo.exe version
```

Должно показать версию Hugo (например: `hugo v0.135.0`)

### 2. Проверка структуры сайта
```powershell
# Проверить наличие конфигурационного файла
Test-Path hugo.toml

# Проверить наличие контента
Test-Path content
```

### 3. Проверка синтаксиса конфигурации
```powershell
.\hugo-bin\hugo.exe config
```

Если ошибок нет, конфигурация корректна.

---

## Остановка сервера

Для остановки локального сервера нажмите:
- `Ctrl + C` в терминале

---

## Решение проблем

### Проблема: Порт 1313 уже занят

**Решение:**
```powershell
.\hugo-bin\hugo.exe server --port 1314
```

### Проблема: Hugo не найден

**Решение:**
Убедитесь, что вы находитесь в правильной директории:
```powershell
cd D:\Code\rehab-site\rehab-site
Get-Location
```

Должно быть: `D:\Code\rehab-site\rehab-site`

### Проблема: Ошибки при сборке

**Решение:**
Проверьте синтаксис:
```powershell
.\hugo-bin\hugo.exe --quiet
```

Это покажет ошибки без запуска сервера.

---

## Проверка мобильной версии

После запуска локального сервера:

1. **В браузере:**
   - Откройте `http://localhost:1313`
   - Нажмите `F12` для открытия инструментов разработчика
   - Нажмите `Ctrl + Shift + M` для режима адаптивного дизайна
   - Выберите устройство (например, iPhone, Samsung Galaxy)

2. **Или через сеть:**
   - Запустите сервер с параметром `--bind 0.0.0.0`
   - Откройте сайт на телефоне: `http://ВАШ_IP:1313`

---

## Полная команда для разработки

```powershell
cd D:\Code\rehab-site\rehab-site
.\hugo-bin\hugo.exe server -D --disableFastRender --bind 0.0.0.0
```

Это запустит сервер:
- ✅ С включением черновиков
- ✅ Без быстрой перезагрузки (для стабильности)
- ✅ Доступный с других устройств в сети

---

## Проверка изменений

После изменений в файлах:
- Страница автоматически перезагрузится в браузере
- Изменения в `.toml` требуют перезапуска сервера
- Изменения в `.html`, `.css`, `.md` применяются автоматически

---

**Готово! Теперь можно разрабатывать и тестировать сайт локально! 🚀**
