# Полное руководство по установке сайта на VPS

## 📋 Данные сервера

- **IP-адрес:** 89.111.163.10
- **Пользователь:** root
- **Пароль:** 7T2G#FPVCDJwUMPN
- **Домен:** narcologia-24.ru
- **Пользователь для деплоя:** deploy
- **Путь сайта:** /var/www/rehab-center

---

## 🚀 Шаг 1: Подключение к серверу и загрузка скрипта

### 1.1 Подключитесь к серверу

**Windows (PowerShell):**
```powershell
ssh root@89.111.163.10
```
Пароль: `7T2G#FPVCDJwUMPN`

### 1.2 Загрузите скрипт настройки

**С локального компьютера (PowerShell):**
```powershell
cd D:\Code\rehab-site\rehab-site
scp scripts\setup-vds.sh root@89.111.163.10:/root/
```
Пароль: `7T2G#FPVCDJwUMPN`

### 1.3 Исправьте окончания строк (ВАЖНО!)

**На сервере:**
```bash
sed -i 's/\r$//' /root/setup-vds.sh
chmod +x /root/setup-vds.sh
```

---

## 🛠️ Шаг 2: Запуск скрипта настройки

### 2.1 Запустите скрипт

```bash
sudo bash /root/setup-vds.sh narcologia-24.ru
```

**Время выполнения:** 5-10 минут

**При запросе email для Let's Encrypt:**
- Введите ваш email для получения уведомлений
- Или нажмите Enter для пропуска (SSL установится позже)

### 2.2 Исправление ошибки Nginx (если возникает)

Если скрипт выдает ошибку Nginx с SSL, выполните:

```bash
sed -i 's/listen 443 ssl http2;/listen 443 http2;/' /etc/nginx/sites-available/rehab-center
sed -i 's/listen \[::\]:443 ssl http2;/listen [::]:443 http2;/' /etc/nginx/sites-available/rehab-center
sed -i '/ssl_certificate/d' /etc/nginx/sites-available/rehab-center
sed -i '/ssl_certificate_key/d' /etc/nginx/sites-available/rehab-center
nginx -t
systemctl reload nginx
```

Затем запустите скрипт снова или выполните оставшиеся шаги вручную.

---

## 🔧 Шаг 3: Настройка домена в Nginx

### 3.1 Обновите домен в конфигурации Nginx

```bash
sed -i 's/rehab-center\.ru/narcologia-24.ru/g' /etc/nginx/sites-available/rehab-center
sed -i 's/www\.rehab-center\.ru/www.narcologia-24.ru/g' /etc/nginx/sites-available/rehab-center
nginx -t
systemctl reload nginx
```

### 3.2 Проверьте статус Nginx

```bash
systemctl status nginx
```

Должен быть `active (running)`

---

## 📁 Шаг 4: Создание тестового файла

### 4.1 Создайте тестовый файл

```bash
mkdir -p /var/www/rehab-center
echo "<h1>Site is working! narcologia-24.ru</h1><p>Nginx работает корректно.</p>" > /var/www/rehab-center/index.html
chown -R deploy:www-data /var/www/rehab-center
chmod -R 755 /var/www/rehab-center
```

### 4.2 Проверьте доступность

```bash
curl http://localhost
curl -I http://89.111.163.10
```

**В браузере откройте:** `http://89.111.163.10` — должен показать страницу с текстом "Site is working!"

---

## 🔑 Шаг 5: Получение SSH ключа для GitHub

### 5.1 Получите приватный SSH ключ

```bash
cat /home/deploy/.ssh/id_rsa
```

**Скопируйте весь вывод** (включая строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`).

Сохраните этот ключ — он понадобится для GitHub Secrets.

---

## 🌐 Шаг 6: Настройка DNS

### 6.1 DNS записи в reg.ru

1. Войдите в личный кабинет reg.ru: https://www.reg.ru/
2. Найдите домен `narcologia-24.ru` в разделе **Мои домены**
3. Откройте домен и перейдите в **DNS-серверы и зона** или **Управление DNS**
4. Проверьте/добавьте A-записи:

   **Запись 1:**
   - Имя: `@` (или пустое)
   - Тип: `A`
   - Значение: `89.111.163.10`
   - TTL: `3600`

   **Запись 2:**
   - Имя: `www`
   - Тип: `A`
   - Значение: `89.111.163.10`
   - TTL: `3600`

5. Сохраните изменения

### 6.2 Проверка DNS

**Windows (PowerShell):**
```powershell
nslookup narcologia-24.ru
nslookup www.narcologia-24.ru
```

Оба должны вернуть: `89.111.163.10`

**Дождитесь обновления DNS:** 5-30 минут (максимум 24 часа)

---

## 🔐 Шаг 7: Настройка GitHub Secrets

### 7.1 Откройте GitHub репозиторий

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret** для каждого секрета

### 7.2 Добавьте секреты:

#### 1. VDS_HOST
- **Имя:** `VDS_HOST`
- **Значение:** `89.111.163.10`

#### 2. VDS_USER
- **Имя:** `VDS_USER`
- **Значение:** `deploy`

#### 3. VDS_PATH
- **Имя:** `VDS_PATH`
- **Значение:** `/var/www/rehab-center`

#### 4. SSH_PRIVATE_KEY
- **Имя:** `SSH_PRIVATE_KEY`
- **Значение:** Весь приватный SSH ключ из шага 5.1 (включая BEGIN и END строки)

#### 5. DOMAIN_NAME
- **Имя:** `DOMAIN_NAME`
- **Значение:** `narcologia-24.ru`

---

## 📦 Шаг 8: Обновление конфигурации проекта

### 8.1 Обновите baseURL в hugo.toml

На локальном компьютере откройте `hugo.toml`:

```toml
baseURL = 'https://narcologia-24.ru/'
```

**Убедитесь, что домен правильный!**

---

## 🚀 Шаг 9: Первый деплой через GitHub Actions

### 9.1 Вариант A: Автоматический деплой

**На локальном компьютере:**
```powershell
cd D:\Code\rehab-site\rehab-site
git add .
git commit -m "Ready for deployment to narcologia-24.ru"
git push origin main
```

GitHub Actions автоматически запустит workflow `Build and Deploy`.

### 9.2 Вариант B: Ручной запуск

1. Перейдите в раздел **Actions** в GitHub репозитории
2. Выберите workflow **Build and Deploy**
3. Нажмите **Run workflow**
4. Выберите ветку `main`
5. Нажмите **Run workflow**

### 9.3 Проверка деплоя

После успешного деплоя:
- Сайт будет доступен по адресу: `http://narcologia-24.ru` (или через IP: `http://89.111.163.10`)
- Проверьте в браузере, что сайт загружается

---

## 🔒 Шаг 10: Установка SSL сертификата

### 10.1 Подготовка

**Убедитесь, что:**
- DNS записи настроены и обновились
- Сайт работает по HTTP
- Домен доступен: `dig narcologia-24.ru +short` возвращает `89.111.163.10`

### 10.2 Установка SSL

**На сервере:**
```bash
ssh root@89.111.163.10
certbot --nginx -d narcologia-24.ru -d www.narcologia-24.ru
```

**Введите:**
- Email для уведомлений
- Подтвердите условия использования Let's Encrypt

Certbot автоматически:
- Установит SSL сертификат
- Настроит Nginx для использования HTTPS
- Настроит автоматическое обновление сертификатов

### 10.3 Проверка SSL

**В браузере откройте:** `https://narcologia-24.ru`
- Должен быть зеленый замочек (валидный SSL сертификат)
- Сайт должен работать по HTTPS

---

## ✅ Шаг 11: Финальная проверка

### 11.1 Проверка на сервере

```bash
ssh root@89.111.163.10

# Статус Nginx
systemctl status nginx

# SSL сертификаты
certbot certificates

# Файлы на сервере
ls -la /var/www/rehab-center

# Логи Nginx
tail -f /var/log/nginx/narcologia-24.ru-access.log
```

### 11.2 Проверка сайта

**Откройте в браузере:**
- `https://narcologia-24.ru` — должен работать с SSL
- `https://www.narcologia-24.ru` — должен работать с SSL
- Все страницы должны загружаться корректно

---

## 🔧 Полезные команды

### Проверка DNS
```powershell
# Windows
nslookup narcologia-24.ru

# Linux/Mac/на сервере
dig narcologia-24.ru +short
```

### Подключение к серверу
```powershell
ssh root@89.111.163.10
```

### Просмотр логов Nginx
```bash
# Access логи
tail -f /var/log/nginx/narcologia-24.ru-access.log

# Error логи
tail -f /var/log/nginx/narcologia-24.ru-error.log
```

### Перезагрузка Nginx
```bash
nginx -t  # Проверка конфигурации
systemctl reload nginx  # Перезагрузка
systemctl restart nginx  # Полный перезапуск
```

### Проверка статуса сервисов
```bash
systemctl status nginx
systemctl status uptime-check.timer
```

---

## 🐛 Решение проблем

### Проблема: Nginx ошибка с SSL

**Решение:**
```bash
sed -i 's/listen 443 ssl http2;/listen 443 http2;/' /etc/nginx/sites-available/rehab-center
sed -i 's/listen \[::\]:443 ssl http2;/listen [::]:443 http2;/' /etc/nginx/sites-available/rehab-center
sed -i '/ssl_certificate/d' /etc/nginx/sites-available/rehab-center
nginx -t && systemctl reload nginx
```

### Проблема: Сайт не работает по домену

**Проверьте:**
1. DNS обновился: `nslookup narcologia-24.ru`
2. Домен в Nginx правильный: `cat /etc/nginx/sites-available/rehab-center | grep server_name`
3. Nginx работает: `systemctl status nginx`

### Проблема: 403 Forbidden

**Решение:**
```bash
chown -R deploy:www-data /var/www/rehab-center
chmod -R 755 /var/www/rehab-center
```

### Проблема: 502 Bad Gateway

**Решение:**
```bash
systemctl restart nginx
nginx -t
tail -50 /var/log/nginx/error.log
```

### Проблема: SSL не устанавливается

**Проверьте:**
1. DNS обновился: `dig narcologia-24.ru +short` должен вернуть IP
2. Порты 80 и 443 открыты: `ufw status`
3. Сайт работает по HTTP: `http://narcologia-24.ru`

---

## 📋 Чеклист финальной проверки

- [ ] VPS настроен (скрипт выполнен)
- [ ] Nginx работает: `systemctl status nginx`
- [ ] Домен в Nginx правильный: `narcologia-24.ru`
- [ ] DNS записи настроены: `nslookup narcologia-24.ru` → `89.111.163.10`
- [ ] GitHub Secrets настроены (5 секретов)
- [ ] `hugo.toml` обновлен: `baseURL = 'https://narcologia-24.ru/'`
- [ ] Первый деплой выполнен через GitHub Actions
- [ ] Файлы на сервере: `ls -la /var/www/rehab-center`
- [ ] Сайт работает по HTTP: `http://narcologia-24.ru`
- [ ] SSL установлен: `certbot certificates`
- [ ] Сайт работает по HTTPS: `https://narcologia-24.ru`

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт должен быть полностью настроен и доступен по адресу:
- **HTTPS:** https://narcologia-24.ru
- **HTTP:** http://narcologia-24.ru (автоматически редиректит на HTTPS)

**Удачного деплоя! 🚀**
