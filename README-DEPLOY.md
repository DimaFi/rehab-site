# Инструкция по деплою и настройке VDS

Этот документ содержит подробные инструкции по настройке VDS сервера и автоматическому деплою Hugo сайта через GitHub Actions.

## Содержание

1. [Требования](#требования)
2. [Настройка VDS сервера](#настройка-vds-сервера)
3. [Настройка GitHub Secrets](#настройка-github-secrets)
4. [Первый деплой](#первый-деплой)
5. [Мониторинг](#мониторинг)
6. [Устранение неполадок](#устранение-неполадок)

## Требования

Перед началом убедитесь, что у вас есть:

- VDS сервер с Ubuntu 20.04+ или 22.04 LTS
- Root доступ к серверу
- Доменное имя, указывающее на IP адрес VDS (для SSL)
- GitHub репозиторий с вашим проектом
- Права на настройку GitHub Secrets в репозитории

## Настройка VDS сервера

### Шаг 1: Подключение к серверу

Подключитесь к вашему VDS серверу по SSH:

```bash
ssh root@your-vds-ip
```

### Шаг 2: Загрузка скрипта настройки

Есть несколько способов загрузить скрипт на сервер:

**Способ 1: Через scp (с локальной машины)**

```bash
scp scripts/setup-vds.sh root@your-vds-ip:/root/
```

**Способ 2: Через wget (напрямую с сервера)**

```bash
# Если скрипт в публичном репозитории GitHub
wget https://raw.githubusercontent.com/your-username/your-repo/main/scripts/setup-vds.sh
chmod +x setup-vds.sh
```

**Способ 3: Создание файла вручную**

```bash
nano setup-vds.sh
# Скопируйте содержимое scripts/setup-vds.sh
```

### Шаг 3: Запуск скрипта настройки

Запустите скрипт с правами root. Вы можете указать доменное имя как аргумент:

```bash
sudo bash setup-vds.sh rehab-center.ru
```

Или без аргумента (будет использовано значение по умолчанию):

```bash
sudo bash setup-vds.sh
```

Скрипт выполнит следующие действия:

1. ✅ Обновит систему
2. ✅ Установит необходимые пакеты (nginx, certbot, rsync и др.)
3. ✅ Настроит часовой пояс (Москва)
4. ✅ Создаст пользователя `deploy` для деплоя
5. ✅ Установит и настроит Nginx
6. ✅ Настроит файрвол UFW (откроет порты 22, 80, 443)
7. ✅ Создаст структуру директорий
8. ✅ Сгенерирует SSH ключи для деплоя
9. ✅ Настроит конфигурацию Nginx
10. ✅ Попытается установить SSL сертификат (требует домен)

### Шаг 4: Установка SSL сертификата

Если скрипт не смог автоматически установить SSL (например, домен еще не указывает на сервер), выполните вручную:

```bash
certbot --nginx -d rehab-center.ru -d www.rehab-center.ru
```

Введите email и подтвердите условия использования Let's Encrypt.

**Важно:** Убедитесь, что:
- Доменное имя `rehab-center.ru` указывает на IP адрес вашего VDS
- Порты 80 и 443 открыты в файрволе
- Nginx запущен и работает

### Шаг 5: Сохранение SSH ключа

После выполнения скрипта, найдите приватный SSH ключ:

```bash
cat /home/deploy/.ssh/id_rsa
```

**Скопируйте весь вывод**, включая строки `-----BEGIN RSA PRIVATE KEY-----` и `-----END RSA PRIVATE KEY-----`. Этот ключ понадобится для настройки GitHub Secrets.

Также можно просмотреть публичный ключ:

```bash
cat /home/deploy/.ssh/id_rsa.pub
```

## Настройка GitHub Secrets

Для работы автоматического деплоя необходимо настроить секреты в GitHub репозитории.

### Шаг 1: Откройте настройки репозитория

1. Перейдите в ваш GitHub репозиторий
2. Нажмите **Settings** (Настройки)
3. В левом меню выберите **Secrets and variables** → **Actions**

### Шаг 2: Добавьте необходимые секреты

Нажмите **New repository secret** и добавьте следующие секреты:

#### 1. `VDS_HOST`
- **Имя:** `VDS_HOST`
- **Значение:** IP адрес или доменное имя вашего VDS сервера
- **Пример:** `123.45.67.89` или `vds.example.com`

#### 2. `VDS_USER`
- **Имя:** `VDS_USER`
- **Значение:** `deploy` (пользователь, созданный скриптом)

#### 3. `VDS_PATH`
- **Имя:** `VDS_PATH`
- **Значение:** `/var/www/rehab-center` (путь, куда деплоить сайт)

#### 4. `SSH_PRIVATE_KEY`
- **Имя:** `SSH_PRIVATE_KEY`
- **Значение:** Приватный SSH ключ (полный вывод из `/home/deploy/.ssh/id_rsa`)
  ```
  -----BEGIN RSA PRIVATE KEY-----
  MIIEpAIBAAKCAQEA...
  (весь ключ целиком)
  ...
  -----END RSA PRIVATE KEY-----
  ```

#### 5. `DOMAIN_NAME` (опционально, для проверки деплоя)
- **Имя:** `DOMAIN_NAME`
- **Значение:** `rehab-center.ru`

### Шаг 3: Проверка секретов

Убедитесь, что все секреты добавлены. Они будут использоваться в GitHub Actions workflow.

## Первый деплой

### Автоматический деплой

После настройки всех секретов, деплой будет происходить автоматически при каждом push в ветку `main`.

1. Внесите изменения в код
2. Закоммитьте и запушьте в `main`:

```bash
git add .
git commit -m "Initial deployment setup"
git push origin main
```

3. Перейдите в раздел **Actions** в GitHub репозитории
4. Следите за выполнением workflow `Build and Deploy`
5. После успешного завершения, ваш сайт будет доступен по адресу `https://rehab-center.ru`

### Ручной запуск деплоя

Вы также можете запустить деплой вручную:

1. Перейдите в **Actions** в GitHub репозитории
2. Выберите workflow **Build and Deploy**
3. Нажмите **Run workflow**
4. Выберите ветку (обычно `main`)
5. Нажмите **Run workflow**

## Мониторинг

### Проверка статуса сайта

Скрипт настройки создал автоматический мониторинг доступности сайта.

**Проверка логов мониторинга:**

```bash
tail -f /var/log/uptime-check/uptime.log
```

**Проверка статуса сервиса мониторинга:**

```bash
systemctl status uptime-check.timer
systemctl status uptime-check.service
```

**Просмотр последних проверок:**

```bash
journalctl -u uptime-check.service -n 50
```

### Ручной запуск проверки

Вы можете вручную запустить скрипт проверки:

```bash
/usr/local/bin/uptime-check.sh rehab-center.ru
```

Или с указанием другого файла логов:

```bash
/usr/local/bin/uptime-check.sh rehab-center.ru /tmp/custom.log
```

### Просмотр логов Nginx

**Access лог:**

```bash
tail -f /var/log/nginx/rehab-center-access.log
```

**Error лог:**

```bash
tail -f /var/log/nginx/rehab-center-error.log
```

**Все логи Nginx:**

```bash
tail -f /var/log/nginx/*.log
```

### Проверка статуса сервисов

```bash
# Статус Nginx
systemctl status nginx

# Статус SSL сертификатов
certbot certificates

# Проверка обновления SSL (должно быть автоматическим)
systemctl status certbot.timer
```

## Устранение неполадок

### Проблема: Деплой не запускается

**Решение:**
1. Проверьте, что все GitHub Secrets настроены правильно
2. Убедитесь, что workflow файл находится в `.github/workflows/deploy.yml`
3. Проверьте логи в разделе **Actions** в GitHub

### Проблема: SSH подключение не работает

**Решение:**
1. Проверьте, что SSH ключ правильно скопирован в GitHub Secrets (включая все строки)
2. Убедитесь, что файрвол разрешает подключение на порт 22
3. Проверьте подключение вручную:
   ```bash
   ssh -i ~/.ssh/id_rsa deploy@your-vds-ip
   ```

### Проблема: Rsync не может записать файлы

**Решение:**
1. Проверьте права на директорию:
   ```bash
   sudo chown -R deploy:www-data /var/www/rehab-center
   sudo chmod -R 775 /var/www/rehab-center
   ```
2. Убедитесь, что пользователь `deploy` добавлен в группу `www-data`:
   ```bash
   sudo usermod -aG www-data deploy
   ```

### Проблема: SSL сертификат не устанавливается

**Решение:**
1. Убедитесь, что домен указывает на IP адрес VDS:
   ```bash
   dig rehab-center.ru +short
   ```
2. Проверьте, что порты 80 и 443 открыты:
   ```bash
   sudo ufw status
   ```
3. Попробуйте установить вручную:
   ```bash
   sudo certbot --nginx -d rehab-center.ru -d www.rehab-center.ru
   ```

### Проблема: Сайт показывает 502 Bad Gateway или 403 Forbidden

**Решение:**
1. Проверьте права на файлы:
   ```bash
   ls -la /var/www/rehab-center
   ```
2. Убедитесь, что Nginx может читать файлы:
   ```bash
   sudo chown -R deploy:www-data /var/www/rehab-center
   sudo chmod -R 755 /var/www/rehab-center
   ```
3. Проверьте конфигурацию Nginx:
   ```bash
   sudo nginx -t
   ```
4. Проверьте логи Nginx на наличие ошибок:
   ```bash
   sudo tail -50 /var/log/nginx/error.log
   ```

### Проблема: Мониторинг не работает

**Решение:**
1. Проверьте, что скрипт существует и исполняемый:
   ```bash
   ls -la /usr/local/bin/uptime-check.sh
   sudo chmod +x /usr/local/bin/uptime-check.sh
   ```
2. Проверьте статус таймера:
   ```bash
   sudo systemctl status uptime-check.timer
   sudo systemctl enable uptime-check.timer
   sudo systemctl start uptime-check.timer
   ```
3. Проверьте логи systemd:
   ```bash
   sudo journalctl -u uptime-check.service -n 50
   ```

## Дополнительные настройки

### Настройка автоматических обновлений системы

Рекомендуется настроить автоматические обновления безопасности:

```bash
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades
```

### Настройка резервного копирования (опционально)

Вы можете настроить автоматическое резервное копирование сайта. Пример cron задачи:

```bash
# Добавить в crontab (crontab -e)
0 2 * * * tar -czf /backup/rehab-center-$(date +\%Y\%m\%d).tar.gz /var/www/rehab-center
```

### Настройка уведомлений

В файле `scripts/uptime-check.sh` есть примеры отправки уведомлений через Telegram или email. Раскомментируйте нужные строки и настройте параметры.

## Полезные команды

```bash
# Перезагрузка Nginx
sudo systemctl reload nginx

# Проверка конфигурации Nginx
sudo nginx -t

# Просмотр активных соединений
sudo netstat -tulpn | grep nginx

# Проверка использования диска
df -h

# Проверка использования памяти
free -h

# Просмотр процессов
htop

# Просмотр последних логинов
last

# Проверка файрвола
sudo ufw status verbose
```

## Безопасность

Рекомендации по безопасности:

1. ✅ Используйте сложные пароли для пользователей
2. ✅ Настройте fail2ban для защиты от брутфорса (уже установлен скриптом)
3. ✅ Регулярно обновляйте систему
4. ✅ Не храните SSH ключи в открытом виде
5. ✅ Используйте только HTTPS
6. ✅ Регулярно проверяйте логи на подозрительную активность
7. ✅ Делайте резервные копии

## Поддержка

При возникновении проблем:

1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Изучите логи в разделе **Actions** в GitHub
3. Проверьте логи на сервере (Nginx, systemd, мониторинг)

---

**Удачных деплоев! 🚀**

